export interface ProductVm {
    id: string;  // UUID string
    title: string;
    description: string;
    condition: string;
    manufacturer: string;
    productionDate: Date;
    imageUrl: string;
}
