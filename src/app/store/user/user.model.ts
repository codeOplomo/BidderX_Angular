export interface User {
    profileIdentifier?: string;
    firstName?: string;
    lastName?: string;
    phoneNumber?: string;
    email?: string;
    imageUrl?: string; 
    coverImageUrl?: string;
    message?: string;
    // Add other properties as needed
  }
  